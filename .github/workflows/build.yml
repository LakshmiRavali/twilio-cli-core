name: Cli-core Buils

on:
  repository_dispatch:
    types: [ releaseCliCore ]
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v2
#     - run: npm install
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
    - run: npm ci
    - run: npm test
#   releseCli:
#     strategy:
#       matrix:
#         repo: ['LakshmiRavali/twilio-cli']
#     runs-on: ubuntu-latest
#     needs: [build]
#     steps:
#       - name: Repository Dispatch
#         uses: peter-evans/repository-dispatch@v1
#         with:
#           token: ${{ secrets.REPO_ACCESS_TOKEN }}
#           repository: ${{ matrix.repo }}
#           event-type: releaseCli
  updateAPIDefinitions:
    runs-on: ubuntu-latest
    steps:
    - run: echo $SHELL
    - name: Checkout
      uses: actions/checkout@v2
      with:
        repository: 'LakshmiRavali/twilio-oai'
        token:  ${{ secrets.OAI_TOKEN }}
    - name: Pushes test file
      uses: dmnemec/copy_file_to_another_repo_action@main
      env:
        API_TOKEN_GITHUB: ${{ secrets.GITHUB_TOKEN }}
      with:
        source_file: 'spec/json/'
        destination_repo: 'LakshmiRavali/twilio-cli-core'
        destination_folder: 'src/services/twilio-api/'
        user_email: 'lakshmiravali.rimmalapudi@gmail.com'
        user_name: 'lakshmiravali'
        commit_message: 'A custom message for the commit'
#     steps:
#     - run: mkdir oai_definitions
#     - run: pwd
#     - run: ls
#     - uses: actions/checkout@v2
#       with:
#         repository: 'LakshmiRavali/twilio-oai'
#         token:  ${{ secrets.OAI_TOKEN }}
#         run: |
#           pwd
#           cp -r spec/json ${{ github.workspace }}/oai_definitions/
#     - uses: actions/checkout@v2
#     - name: Update OAI folder
#       run: |
#         ls
#         rm -f src/services/twilio-api/twilio*.json
#         cp -r oai_definitions src/services/twilio-api/
#         git status
        
